# Теория Go (Golang)

## 📦 Пакеты

### `fmt`
`fmt` - стандартный пакет базовых команд для ввода/вывода

---

## 🔢 Примитивные типы данных

- **Числовые типы**: `int`, `float`
- **Строчный тип**: `string`
- **Логический тип**: `bool`

---

## 📝 Переменные

### Объявление переменных

- **`var`** - объявляет создание новой переменной:
  - Выделяет память
  - Назначает неизменяемый тип
  - Дает значение по умолчанию, если не указано

- **Короткая запись `:=`** без явного указания типа - идиоматична для Go

- ⚠️ Код не запустится, если есть неиспользованная переменная

---

## 🔤 Строки

В Go строки - это массив значений типа `byte`.

### Особенности

- Строки неизменяемы
- К элементам можно обращаться по индексу

### Методы работы со строками

- **`len()`** - длина строки
  
  ⚠️ **Нюанс**: длина в байтах не всегда корректна, т.к. Go может работать с Unicode, в которой не все символы умещаются в один байт

- **`utf8.RuneCountInString(s)`** - специальная команда для длины строки из пакета `utf8` для Unicode

- **Raw строки**: если заключить строку в обратные кавычки (`` ` ``), все будет отформатировано как есть, включая управляющие последовательности

---

## 🎯 Пользовательские типы

```go:
type Fruit string
var fruit Fruit
```

Для пользовательских типов можно определять методы (а для встроенных нельзя).

---

## 🔄 Приведение типов

Чтобы привести один тип к другому:

```go:
type(variable)
```

---

## 🏷️ Алиасы типов

Абсолютно для всех типов можно использовать алиасы.

---

*Материал структурирован для лучшего восприятия с использованием Markdown разметки*

